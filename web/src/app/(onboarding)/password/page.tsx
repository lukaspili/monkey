import { RequestPasswordResetView } from "@/components/request-password-reset/RequestPasswordResetView";

export default function RequestPasswordResetPage() {
  return <RequestPasswordResetView />;
}
