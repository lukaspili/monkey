"use server";

// export async function getSessionUserOrFail(): Promise<SessionUser> {
//   const response = await api<User>({ path: "me" });
//   if (!response.successful) {
//     throw new Error("Failed to retrieve current user");
//   }

//   return response.data!;
// }
